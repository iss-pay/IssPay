<div class="container-fluid">
  <div class="row p-4 mt-1">
    <%= render :'shared/item_form' %>
  </div>
  <div class="row mt-2 justify-content-center">
    <div class="col-6">
      <table class="table Drinks">
        <thead>
          <tr>
            <th scope='col'> # </th>
            <% item.attributes.each do |attr| %>
              <th scope='col'> <%= attr %> </th>
            <% end %>
            <th scope='col'>Action</th>
          </tr>
        </thead>
        <tbody>
          <% item.drinks.each_with_index do |drink, index| %>
            <tr id="<%=drink.id%>">
              <th scope='row'> <%= index+1 %> </th>
              <td><%= drink.name %></td>
              <td><%= drink.quantity %></td>
              <td><%= drink.price %></td>
              <td> <img src="<%= drink.image_url %>" width="60" height="60"></td>
              <td> Drink </td>
              <td>
                <div class="btn-group-sm btn-group" role='group'>
                  <button class='btn btn-success'>Edit</button>
                  <button class='btn btn-success'>Delete</button>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="col-6">
      <table class="table Snacks">
        <thead>
          <tr>
            <th scope='col'> # </th>
            <% item.attributes.each do |attr| %>
              <th scope='col'> <%= attr %> </th>
            <% end %>
            <th scope='col'>Action</th>
          </tr>
        </thead>
                <tbody>
          <% item.snacks.each_with_index do |snack, index| %>
            <tr id="<%=snack.id%>">
              <th scope='row'> <%= index+1 %> </th>
              <td><%= snack.name %></td>
              <td><%= snack.quantity %></td>
              <td><%= snack.price %></td>
              <td> <img src="<%= snack.image_url %>" width="60" height="60"></td>
              <td> Snack </td>
              <td>
                <div class="btn-group-sm btn-group" role='group'>
                  <button class='btn btn-success'>Edit</button>
                  <button class='btn btn-success'>Delete</button>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>